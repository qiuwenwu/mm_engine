module.exports = {
	/**
	 * 安装
	 * @param {object} e 管理器
	 */
	install(e) {

	},
	/**
	 * 卸载
	 * @param {object} e 管理器
	 */
	unInstall(e) {
	
	},
	/**
	 * 更新
	 * @param {object} e 管理器
	 */
	update(e) {
	
	},
	/**
	 * 初始化
	 * @param {object} e 管理器
	 */
	init(e) {
	
	},
	/**
	 * 初始化
	 * @param {object} e 管理器
	 */
	start(e) {
	
	},
	/**
	 * 暂停
	 * @param {object} e 管理器
	 */
	stop(e) {
	
	},
	/**
	 * 结束
	 * @param {object} e 管理器
	 */
	stop(e) {
	
	},
	/**
	 * 移除
	 * @param {object} e 管理器
	 */
	remove(e) {
	
	}
}